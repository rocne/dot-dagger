# .dotd.yaml (legacy)

`.dotd.yaml` was the previous name for the per-directory configuration file. It has been renamed to [`.dagger`](dagger.md).

If you have existing `.dotd.yaml` files, rename them to `.dagger`. The format is otherwise identical.
