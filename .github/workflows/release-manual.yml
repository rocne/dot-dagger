name: Release dotd

# Break-glass release path: push a tag (e.g. `git push origin v0.6.1`) to cut a
# release by hand. The primary path is release-please.yml (merge the standing
# release PR); use this only when that can't run — e.g. re-cutting a botched
# release.
#
# Delegates the actual release + e2e to the central reusable workflow
# (rocne/release-ci) so the manual and release-please paths can never drift.

on:
  push:
    tags:
      - "v*"

jobs:
  release:
    permissions:
      contents: write
      issues: write
      id-token: write
      attestations: write
    uses: rocne/release-ci/.github/workflows/release.yml@v0.1.5
    with:
      version: ${{ github.ref_name }}
      goreleaser_config: .goreleaser/dotd.yaml
      project_name: dotd
      package_name: dot-dagger
      binary: dotd
      e2e_script: ./test/run-e2e-release.sh
    secrets: inherit
