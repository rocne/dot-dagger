version: 2

project_name: dotd

before:
  hooks:
    - go mod tidy

builds:
  - id: dotd
    binary: dotd
    main: ./cmd/dotd
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{.Env.VERSION}} -X main.commit={{.Commit}} -X main.date={{.Date}}

archives:
  - id: dotd
    ids: [dotd]
    name_template: "{{ .ProjectName }}_{{ .Tag }}_{{ .Os }}_{{ .Arch }}"
    formats: [tar.gz]

nfpms:
  - id: dotd
    # Package name (install identity in shared apt/dnf/brew namespaces) is the
    # disambiguated project name; the binary it installs is the short `dotd`.
    package_name: dot-dagger
    ids: [dotd]
    file_name_template: "{{ .ConventionalFileName }}"
    formats:
      - deb
      - rpm
    maintainer: Rocne Scribner <rocne.ks@gmail.com>
    homepage: https://github.com/rocne/dot-dagger
    description: Dotfiles manager — env resolution, DAG, symlinks, and packages
    license: MIT
    bindir: /usr/bin
    rpm:
      signature:
        key_file: "{{ .Env.GPG_KEY_PATH }}"
    deb:
      signature:
        key_file: "{{ .Env.GPG_KEY_PATH }}"

homebrew_casks:
  - name: dot-dagger
    ids: [dotd]
    repository:
      owner: rocne
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    homepage: https://github.com/rocne/dot-dagger
    description: Dotfiles manager — env resolution, DAG, symlinks, and packages
    license: MIT
    # Binaries are not notarized; strip the Gatekeeper quarantine xattr on install
    # so the cask-installed dotd runs without "cannot be opened" errors on macOS.
    hooks:
      post:
        install: |
          if system_command("/usr/bin/xattr", args: ["-h"]).exit_status == 0
            system_command "/usr/bin/xattr", args: ["-dr", "com.apple.quarantine", "#{staged_path}/dotd"]
          end

# Publish the signed .deb/.rpm to the Cloudsmith-hosted apt/dnf repo, giving
# `apt install dot-dagger` / `dnf install dot-dagger` by name with auto-update.
# (GoReleaser's native `cloudsmiths` block is Pro-only, so we drive the
# cloudsmith CLI via a custom publisher instead.) Repo-index signing is handled
# server-side by a Cloudsmith-managed key (rocne/releases) — separate from the
# package-signing key, which never leaves our control. Cloudsmith serves that
# index key's public half via its setup scripts, so it is not committed here.
#
# Publishers iterate every linux package and archive artifact, so the command
# dispatches on extension and pushes only deb/rpm — tar.gz archives are skipped.
# any-distro/any-version makes one upload installable across every apt/dnf distro
# rather than maintaining a per-distro matrix.
publishers:
  - name: cloudsmith
    ids: [dotd]
    cmd: >-
      sh -c 'case "{{ .ArtifactName }}" in
      *.deb) exec cloudsmith push deb --republish ${CLOUDSMITH_REPO}/any-distro/any-version "{{ .ArtifactPath }}" ;;
      *.rpm) exec cloudsmith push rpm --republish ${CLOUDSMITH_REPO}/any-distro/any-version "{{ .ArtifactPath }}" ;;
      *) true ;;
      esac'
    env:
      - CLOUDSMITH_API_KEY={{ .Env.CLOUDSMITH_API_KEY }}
      - CLOUDSMITH_REPO={{ .Env.CLOUDSMITH_REPO }}

checksum:
  name_template: "{{ .ProjectName }}_{{ .Tag }}_checksums.txt"

signs:
  - cmd: cosign
    artifacts: checksum
    # cosign v3 signs into a single Sigstore bundle (signature + Fulcio cert);
    # the old detached .sig/.pem pair is gone (rocne/release-ci#45, D35).
    signature: "${artifact}.sigstore.json"
    args:
      - sign-blob
      - "--bundle=${signature}"
      - "${artifact}"
      - "--yes"

release:
  extra_files:
    - glob: ./dotd-signing-key.asc

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
