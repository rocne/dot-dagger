# Handoff — 2026-05-24

## What happened this session

Three audit passes ran back-to-back, each cleaning up a different layer of the codebase.

### PR #71 — Philosophy audit (merged)

Eliminated magic strings, duplicated code, and canonical resolution violations:

- `internal/pipeline/walk.go` — exported `ActionCompose/Source/NoSource/Link` constants and unexported annotation key constants; wired in `ecosystem.ConfigFile` / `ecosystem.LegacyConfigFile`
- `internal/pipeline/act.go` — exported `ComposeFileName(dirPath string) string`; was duplicated between act.go and compose_cmd.go with a bug in the compose_cmd.go copy
- `internal/pipeline/initgen.go` — added `GenerateOptions{BinDir, HomeDir}`; init.sh now prepends `export PATH="..."` when BinDir set; `portablePath()` emits `${HOME}/...` notation
- `internal/fileutil/fileutil.go` (new) — `SaveYAML(path, v)`: atomic YAML write extracted from identical blocks in `config.Save` and `env.Save`
- `cmd/dotd/compose_cmd.go` — removed local `genNameFromDir`, replaced with `pipeline.ComposeFileName`

### PR #72 — Output routing audit (merged)

Every user-facing print now goes through `cmd.OutOrStdout()` and `ui.*` color helpers. Fixed `adopt.go` dry-run going to `os.Stdout`; colorized teardown, unapply, check symlink summary.

### PR #73 — Print helper consolidation (merged)

Added to `internal/ui/ui.go`:

| Helper | Output |
|--------|--------|
| `Warnf(w, fmt, ...)` | yellow `warning:` prefix |
| `Errf(w, fmt, ...)` | red `error:` prefix |
| `Tipf(w, fmt, ...)` | cyan `tip:` prefix |
| `OKf(w, fmt, ...)` | green whole line |
| `Skipf(w, fmt, ...)` | faint whole line |
| `Headerf(w, fmt, ...)` | bold section header with leading blank line |

Added `cmd/dotd/prompts.go` — `promptConfirm(out, r)` extracts duplicated `Proceed? [y/N]:` block from teardown and unapply. All call sites in teardown, unapply, setup, init migrated.

## Current state

`main` is clean at v0.2.31 (auto-released after PR #73 merged). No open PRs.

## Immediate next step

None — the codebase is clean. See `.claude/docs/audit-guide.md` for the five-pass methodology if continuing audit work. Passes 1–3 are clean; passes 4–5 have not been run against `internal/` packages.

## Key files added this session

| File | Role |
|------|------|
| `internal/ui/ui.go` | All color helpers + Warnf/Errf/Tipf/OKf/Skipf/Headerf |
| `cmd/dotd/prompts.go` | `promptConfirm` — shared [y/N] prompt |
| `internal/fileutil/fileutil.go` | `SaveYAML` — atomic YAML write |
| `internal/pipeline/walk.go` | Action type and annotation key constants |
| `internal/pipeline/act.go` | `ComposeFileName` |
| `.claude/docs/audit-guide.md` | Audit philosophy + five-pass methodology (local only) |
