# Handoff — 2026-05-24

## What happened this session

Three audit passes ran back-to-back, each cleaning up a different layer of the codebase.

### PR #71 — Philosophy audit (merged)
`feature/claude-philosophy-audit`

Eliminated magic strings, duplicated code, and canonical resolution violations throughout:

- **`internal/pipeline/walk.go`** — exported `ActionCompose/Source/NoSource/Link` constants and unexported annotation key constants (`annAction`, `annAfter`, etc.); replaced all string literals with them; wired in `ecosystem.ConfigFile` / `ecosystem.LegacyConfigFile` for config file name matching
- **`internal/pipeline/act.go`** — exported `ComposeFileName(dirPath string) string` to canonicalise the nosync-/dot-/.d stripping logic (was duplicated between act.go and compose_cmd.go, with a bug in the compose_cmd.go copy)
- **`internal/pipeline/actions.go`** — all `case "action":` etc. replaced with constants
- **`internal/pipeline/initgen.go`** — added `GenerateOptions{BinDir, HomeDir}` to `Generate()`; `buildInitSh` now prepends `export PATH="..."` when BinDir is set; added `portablePath()` to emit `${HOME}/...` notation for paths under home
- **`internal/pipeline/initgen_test.go`** — updated all callers to new signature; added 3 new tests for PATH prepend behaviour
- **`internal/fileutil/fileutil.go`** (new) — `SaveYAML(path, v)`: atomic YAML write extracted from identical 25-line blocks in `config.Save` and `env.Save`
- **`internal/config/config.go`**, **`internal/env/env.go`** — `Save` simplified to one-liner wrapping `fileutil.SaveYAML`
- **`internal/adopter/adopter.go`** — action type strings replaced with `pipeline.Action*` constants
- **`cmd/dotd/compose_cmd.go`** — removed local `genNameFromDir`, replaced with `pipeline.ComposeFileName`; colourised missing/stale labels
- **`cmd/dotd/main.go`** — `Generate` call updated to pass `GenerateOptions{BinDir, HomeDir}`

### PR #72 — Output routing audit (merged)
`feature/claude-output-audit`

Ensured every user-facing print goes through `cmd.OutOrStdout()` and uses `ui.*` colour helpers:

- **`cmd/dotd/adopt.go`** — dry-run output was going to `os.Stdout`; fixed to `cmd.OutOrStdout()`
- **`cmd/dotd/compose_cmd.go`** — missing/stale labels colourised
- **`cmd/dotd/teardown_cmd.go`** — added `ui` import; warnings, preview header, skip/removed/cancelled labels all colourised
- **`cmd/dotd/unapply_cmd.go`** — same treatment
- **`cmd/dotd/main.go`** — ok/missing/wrong counts in `dotd check` symlink summary colourised

### PR #73 — Print helper consolidation (merged)
`feature/claude-ui-print-helpers`

Extracted the repeated assembly patterns into first-class helpers so call sites read like intent:

**`internal/ui/ui.go`** additions:

| Helper | Output |
|--------|--------|
| `Warnf(w, fmt, ...)` | yellow `warning:` prefix + message |
| `Errf(w, fmt, ...)` | red `error:` prefix + message |
| `Tipf(w, fmt, ...)` | cyan `tip:` prefix + message |
| `OKf(w, fmt, ...)` | green whole line |
| `Skipf(w, fmt, ...)` | faint whole line |
| `Headerf(w, fmt, ...)` | bold section header with leading blank line |

**`cmd/dotd/prompts.go`** (new) — `promptConfirm(out io.Writer, r io.Reader) bool`: extracted the duplicated `\nProceed? [y/N]:` block from teardown and unapply.

All call sites in teardown, unapply, setup, and init migrated. `bufio`/`strings` imports dropped from teardown and unapply (no longer needed after extraction).

### PR #74 — Audit guide (open)
`feature/claude-audit-guide`

**`.claude/docs/audit-guide.md`** (new) — documents the three code quality principles and a five-pass grep-driven methodology so future audit sessions can run the same process without re-deriving it. Wired into `CLAUDE.md` spec table.

---

## Current state

| Branch | PR | Status |
|--------|----|--------|
| `feature/claude-philosophy-audit` | #71 | Merged |
| `feature/claude-output-audit` | #72 | Merged |
| `feature/claude-ui-print-helpers` | #73 | Merged |
| `feature/claude-audit-guide` | #74 | **Open** |

`main` is at the merge of PR #73 (`f58b0f2`). Auto-release fired `v0.2.31`.

---

## Immediate next step

Merge PR #74 (docs only, no code change, no release impact).

---

## What the audit guide says to do next

`.claude/docs/audit-guide.md` defines five passes. Passes 1–3 (magic values, duplicated logic, output routing) are now clean. Passes 4–5 (uncolorised output, inline Sprint helpers) are largely clean in `cmd/dotd/` but have not been run against the rest of the codebase.

Known remaining areas that were audited but intentionally left alone:
- `config_cmd.go` / `env_cmd.go` — `c.Stdout = os.Stdout` is a legitimate subprocess exception (interactive editor)
- `dag_cmd.go`, `list_cmd.go`, `package_cmd.go`, `env_cmd.go` — machine-parseable data output; plain `fmt.Fprintf` is correct
- `cfg.log.*` calls in `main.go` — structured logger; `ui.Header()` inline is intentional, not a violation

---

## Key files to know

| File | Role |
|------|------|
| `internal/ui/ui.go` | All colour Sprint functions + Warnf/Errf/Tipf/OKf/Skipf/Headerf print helpers |
| `cmd/dotd/prompts.go` | `promptConfirm` — shared [y/N] prompt |
| `internal/fileutil/fileutil.go` | `SaveYAML` — atomic YAML write |
| `internal/pipeline/walk.go` | Action type and annotation key constants |
| `internal/pipeline/act.go` | `ComposeFileName` — canonical compose output name |
| `.claude/docs/audit-guide.md` | Audit philosophy + five-pass methodology |
