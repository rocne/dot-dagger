# Handoff — 2026-06-07e

## Branch

`feature/claude-prompt-io-consolidation`

PR #93 open — stacked on `feature/claude-annotate-wizard` (PR #92).

## What happened this session

Implemented the 6-task prompt I/O consolidation plan via subagent-driven development. All 6 tasks complete, reviewed (spec + quality), and merged into 7 commits.

### Commits (oldest → newest)

1. `0ed592b` refactor(prompts): add canonical huh helpers with cobra I/O routing
2. `f35e7ef` refactor(filter_prompt): remove huh, use promptInputs, fix os.Stderr
3. `1b7403f` refactor(adopt): remove direct huh usage, use promptBool
4. `daddef6` refactor(annotate): remove direct huh usage, route all I/O through prompts helpers; remove isTTYStdin bridge
5. `59e8aee` test(annotate): replace non-TTY error test with 4 wizard integration tests
6. `d86de45` test(e2e): add annotate Docker e2e tests
7. `fe277e1` fix(prompts): move byteReader into newHuhForm; simplify test and e2e inputs

### What changed

- `prompts.go` — new helpers: `isTTY`, `byteReader`, `newHuhForm`, `promptMenu`, `promptSelect`, `promptInput`, `promptBool`, `promptInputs`; `errUserAborted` moved here
- `filter_prompt.go` — removed huh import, `prompter` var, `isTTYStdin`; `filterWithPrompt` now takes `cmd *cobra.Command`; uses `promptInputs` for missing-key prompting
- `adopt.go` — removed huh import; `promptAdoptConfirm` deleted; uses `promptBool`
- `annotate_cmd.go` — removed huh import, TTY guard; all 4 huh calls replaced with helpers; Long description updated
- `integration_test.go` — `TestAnnotate_NonTTY` replaced with 4 wizard tests (AddWhen, SetAction, SetDisable, CancelAtConfirm)
- `test/e2e/annotate.sh` — new e2e with 3 tests; uses plain `printf | dotd annotate`
- Walking cmd threading: `walkOrdered` and `runPipeline` gained `cmd *cobra.Command` param, cascading to ~7 call sites

### Key technical facts

- `byteReader` in `prompts.go` prevents bufio.Scanner over-read across sequential huh forms in accessible mode — required for both tests and e2e to work with piped stdin
- `isTTY(cmd.InOrStdin())` returns false for `strings.Reader` (no `Fd()` method) — accessible mode triggers automatically
- Only `prompts.go` imports huh: `grep -rn '"github.com/charmbracelet/huh"' cmd/dotd/*.go`
- Registry order: 1=When, 2=After, 3=Require, 4=Request, 5=Action, 6=Name, 7=Disable, 8=Done

## Status

PR #93 is open and **will be iterated on** — expect review feedback before merge.

## Next step

Respond to PR review feedback on #93. When #93 merges, #92 (annotate wizard) should merge first or be re-based. Both PRs are stacked: #92 → #93 → main.
