# Handoff — 2026-05-26

## Session summary

Full test audit followed by targeted fixes. Nothing in Go unit tests warranted changes. Five e2e shell script bugs fixed, committed, PR merged, released.

## What was done

### Test audit

Audited all Go unit tests and e2e shell scripts. Go unit tests are adequate-to-good across the board — no bugs, some edge-case gaps but nothing blocking. E2e scripts had real bugs.

### Bugs fixed (PR #81, v0.2.36)

| File | Bug |
|------|-----|
| `test/e2e/conflict.sh` | Without-`--force` section used `\|\| true` + INFO-only branches — tested nothing. Fixed: now asserts apply exits non-zero and plain file is not replaced. Added `readlink` target check to `--force` path. |
| `test/e2e/binary.sh` | Only checked symlink existence (`test -L`), not target. Fixed: added exact `readlink` match for `.zshrc → /fixture/conf/dot-zshrc`. |
| `test/e2e/symlinks-nested.sh` | All 3 symlink assertions were existence-only. Fixed: exact target checks for `.zshrc`, `.gitconfig`, `.config/nvim/init.lua`. |
| `test/e2e/context.sh` | `.gitconfig` checked as symlink but target not verified. Fixed: added `readlink` check in personal context. |
| `test/e2e/bin.sh` | Target check used `grep -q "bin/hello"` (substring). Fixed: exact `[ "$TARGET" = "/fixture/bin/hello" ]`. |

## Known gaps (not fixed, require spec decisions or bigger scope)

- Several `cmd/dotd/integration_test.go` tests are `t.Skip`ped: `package generate`, `package check`, all compose tests. These reflect unimplemented commands — not test bugs.
- `check.sh` only tests happy path. No negative case (check fails when symlink missing).
- Go unit edge cases: circular `@after`, permission errors on symlink creation, path traversal — low priority, none blocking.

## State at handoff

- Branch: `main` (PR #81 merged)
- Latest release: `v0.2.36`
- All CI green
- `TODO.md` has minor outstanding items (Enable GitHub Pages, TTY-aware missing-key prompt)
