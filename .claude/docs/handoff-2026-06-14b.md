# Handoff — 2026-06-14b (roots-model SHIPPED)

Session 2026-06-14 ~00:30 → ~07:10 UTC. **Local only — do not commit.**

## Headline: the 🔴 top-priority roots-model work is DONE and merged.

PR **#119** squash-merged to `main` (`207e05f`), auto-release **v0.4.10**. Branch
`feature/claude-roots-model` deleted. CI green (test + lint + **e2e**). The
`project_link_root_overhaul` memory has been deleted (work complete).

### What shipped (pure-XDG roots model)
Killed the global `link_root` knob that let config silently redefine `~`.
- Anchor tokens, all knob-less / env-resolved: `~`=$HOME, `$bin`=
  `($XDG_BIN_HOME ?: ~/.local/bin)/dot-dagger` (added to PATH), `$config`=
  `$XDG_CONFIG_HOME ?: ~/.config`.
- Removed every path knob + flag + env + config field: `--link-root`/`--bin-dir`/
  `--init-file`/`--generated-dir`, `DOTD_LINK_ROOT`/`DOTD_BIN_DIR`/etc.,
  `link_root`/`bin_dir`/`generated_dir`. Tool output (generated, init.sh) is fixed
  under `$XDG_DATA_HOME/dot-dagger/`.
- Renamed tool's-own-file flags: `--config`→`--dotd-config`, `--env-file`→
  `--dotd-env` (envs unchanged: `DOTD_CONFIG_FILE`/`DOTD_ENV_FILE`).
- config.yaml shrank to a single `dotfiles` field; `setup` reduced to that prompt.
- Added: anchor-token **validation** (`validateAnchor` — unknown `~x`/`$x` is a
  hard error, no more silent literal links) and **`dotd paths`** (read-only
  resolved-view, `--json`).
- No backward compatibility. Tests configure `HOME`/`XDG_*`, never magic flags.
- Full design/plan committed under `docs/superpowers/{specs,plans}/2026-06-13-roots-model*`.

### How it was built (process the user enforces — keep doing this)
design → challenge → plan → challenge → subagent-driven execution (7 tasks, fresh
implementer + spec-review + code-quality-review per task). Reviews caught real
defects (ConfigDir/XdgConfigHome duplication; 4 commands re-calling
`ecosystem.Home()` instead of reading `cfg.home`; stale `config` help). See
`feedback_design_phase_discipline.md` memory: respect explicit phase order, don't
jump ahead, engage substance when the user reasons aloud.

## Open follow-ups from the roots work (non-blocking, in `.claude/TODO.md`)
- **`internal/setup/shell.go` fish branch** calls `ecosystem.XdgConfigHome()`
  directly instead of receiving `cfg.configDir`. Functionally identical (no
  override tier) so cosmetic, but violates the canonical-resolution pattern the PR
  established. Fix = thread a `configDir` param through `DetectShellConfig` and its
  two callers (`init_cmd.go`, `teardown_cmd.go`). Same file has stale
  `cfg.linkRoot`→`cfg.home` doc comments (pre-existing).
- **Repo-wide gofmt drift** (~15 untouched files, pre-existing) deliberately left
  out of the PR to keep the diff clean. One-shot `gofmt -w` still deferred.

## Other pending items (from `.claude/TODO.md`, untouched this session)
- `CONTRIBUTING.md` (defer until external contributors).
- **Enable GitHub Pages** — Settings → Pages → Source: GitHub Actions (user action;
  `.github/workflows/docs.yml` already deploys on merge).
- **TTY-aware missing-key prompt (M3)** — currently halts with hint; no interactive
  fallback. Spec exists: `docs/superpowers/specs/2026-05-31-tty-missing-key-prompt-design.md`.
- `dotd concepts` sub-topic routing (`dotd concepts when`, etc.) once flat version
  validated.
- **Fedora e2e** — only Ubuntu e2e wired today; Fedora deferred.

## State at handoff
`main` @ `207e05f`, clean, no open PRs, released v0.4.10. Untracked `.claude/docs/`
handoffs/audits are intentionally local. Caveman mode was active all session
(cosmetic only).
